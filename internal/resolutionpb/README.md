# resolutionpb — vendored nn-account resolution contract

A copy of `nn-account/proto/resolution/v1` (generated stubs + the `.proto`), for the same
reason as `../accountpb`: the sibling repository is not reachable from a clean checkout.
To refresh: re-copy the files.
